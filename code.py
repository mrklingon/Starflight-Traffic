# Launches the DrDroid shell
#
# This is done in a sperate file so that __name__ is set to "DrDroid" instead of "__main__"
# which helps some modules detect that they are running in DrDroid.

import startraffic
